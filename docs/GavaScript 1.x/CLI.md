# CLI

```text
用法:
  gs
  gs <文件>         执行指定的 GS 脚本文件
  gs <选项>         执行命令

选项:
  -h, --help        显示帮助信息
  -v, --version     显示程序版本信息
  -upd              检查解释器更新

命令:
  plugin            插件管理系统（开发中）

示例:
  gs.exe demo.gs    执行 demo.gs 脚本
  gs.exe -upd       检查解释器更新
```